"""Crabspy library — computer vision and measurement (rebuild).

Legacy scripts in this package (e.g. ``measure.py``) still use ``import methods``;
new code should import from ``crabspy.io``, ``crabspy.measurement``, etc. as they appear.
"""

from __future__ import annotations

__version__ = "0.2.0"
